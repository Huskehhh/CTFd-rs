---
kind: pipeline
type: docker
name: default

steps:
- name: restore-cache-with-filesystem
  image: meltwater/drone-cache
  pull: true
  settings:
    backend: "filesystem"
    restore: true
    cache_key: "volume"
    archive_format: "gzip"
    filesystem_cache_root: "/var/lib/cache"
    mount:
      - "target"
      - "frontend/node_modules"
  volumes:
    - name: cache
      path: "/var/lib/cache"

- name: build-and-test-rust
  image: registry.husk.pro/just-rust-docker:1.56
  environment:
    HTB_TEAM_ID:
      from_secret: HTB_TEAM_ID
    HTB_EMAIL:
      from_secret: HTB_EMAIL
    HTB_PASSWORD:
      from_secret: HTB_PASSWORD
  commands:
  - just check
  - just test
  - just udeps
  depends_on: [ clone, restore-cache-with-filesystem ] 

- name: build-frontend
  image: node:16
  commands:
  - cd frontend
  - npm install
  - npm run build
  depends_on: [ clone ]

- name: rebuild-cache-with-filesystem
  image: meltwater/drone-cache
  pull: true
  depends_on: [ clone, build-and-test-rust ]
  settings:
    backend: "filesystem"
    rebuild: true
    cache_key: "volume"
    archive_format: "gzip"
    filesystem_cache_root: "/var/lib/cache"    
    mount:
      - "target"
      - "frontend/node_modules"
  volumes:
    - name: cache
      path: "/var/lib/cache"

volumes:
  - name: cache
    host:
      path: "/var/lib/cache"

---
kind: signature
hmac: 449ded01a6bd9c187024ddb6b21a02b12f3e4d8072ebb157d4ba8aa3c4ca6d18

...
